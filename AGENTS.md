# 開発・プロセス＆音声再生管理ルール (AGENTS.md)

1. **日本語対応**: 基本的に日本語で対応する。
2. **適切なタイミングでの Git Push**: 変更完了時に Git コミットおよび Push を行う。

3. **音声読み上げ（TTS）制御ルール**:
   - **Google Translate TTS（`zh-TW`）を最優先エンジンとして使用する**:
     - `https://translate.google.com/translate_tts?ie=UTF-8&q={text}&tl=zh-TW&client=tw-ob` を第1優先で再生すること。
   - **女性ボイスに統一・固定する**:
     - ユーザーの好みに合わせ、クリアで標準的な「台湾華語の女性ボイス」で統一・安定して再生される状態を維持すること。
   - **音声生成エラー（無音）の事前確認とマッピング（`ZHUYIN_SPEECH_MAP`）**:
     - 注音符号単体や異体字によって「音声生成エラー（無音）」が発生しないよう事前検証する。
     - Google TTSで100%確実に綺麗な女性音声として鳴る同音テキストマッピング（例: `ㄟ` ➔ `黑`, `ㄠ` ➔ `凹`）を常時維持すること。

4. **体系的なナンバリングルール**:
   - 注音符号セクション全体を「1」とし、サブステップを「1-1」「1-2」「1-3」のように階層化されたナンバリング表現で統一すること。

5. **バックグラウンドプロセスの厳格管理**:
   - `npm run dev` などの開発サーバーを新たに起動する際は、過去に立ち上げた同種のバックグラウンドタスク（`manage_task`）を必ず先に停止 (`kill`) し、無駄なプロセスが重複して残留しないようにすること。
