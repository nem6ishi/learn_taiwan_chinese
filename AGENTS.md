# 開発・プロセス＆音声再生管理ルール (AGENTS.md)

1. **日本語対応**: 基本的に日本語で対応する。
2. **適切なタイミングでの Git Push**: 変更完了時に Git コミットおよび Push を行う。

3. **音声読み上げ（TTS）制御ルール（Mac Chrome & GitHub Pages 完全対応確定仕様）**:
   - **「Google 國語（臺灣）」/「Meijia」女性ボイスを最優先指定して再生する**:
     - Google TTS の直リンク URL（`client=tw-ob` / `client=gtx`）は Mac Chrome や GitHub Pages (HTTPS) の CORS / 403 Forbidden セキュリティ制限により遮断される。
     - そのため、ブラウザ（Mac Chrome / iOS Safari 等）に標準搭載されている Google 公式の最高品質ボイス **`Google 國語（臺灣）`** および **`Meijia` (zh-TW)** を最優先ターゲットとして指定した `Web Speech API` (`speechSynthesis`) で内部再生させること。
     - これにより、403 / CORS 通信エラーを 100% 原理的に回避しつつ、Google Translate と全く同一の最高品質女性ボイスでクリア・確実・即時に発音される状態を永久に維持すること。
   - **女性ボイスに統一・固定する**:
     - ユーザーの好みに合わせ、クリアで標準的な「台湾華語の女性ボイス（Google 國語 / Meijia）」で統一・安定して再生される状態を維持すること。
   - **音声生成エラー（無音）の事前確認とマッピング（`ZHUYIN_SPEECH_MAP`）**:
     - 注音符号単体や異体字によって「音声生成エラー（無音）」が発生しないよう事前検証する。
     - `ZHUYIN_SPEECH_MAP`（例: `ㄅ` ➔ `包`, `ㄆ` ➔ `婆`, `ㄇ` ➔ `摸`, `ㄈ` ➔ `發`, `ㄟ` ➔ `黑` 等）の漢字置換を常時適用維持すること。

4. **体系的なナンバリングルール**:
   - 注音符号セクション全体を「1」とし、サブステップを「1-1」「1-2」「1-3」のように階層化されたナンバリング表現で統一すること。

5. **バックグラウンドタスク・プロセスの厳格管理と重複防止**:
   - 開発サーバー、監視プロセス、タイマー等の非同期・バックグラウンドタスクを新たに起動・実行する際は、事前に実行中の同種タスクの有無を `manage_task` で確認し、不要な重複タスクやリソースの無駄な残留を防ぐために既存タスクを適切に停止 (`kill`) または整理した上で起動すること。

6. **全自動・自律進行ルール（確認不要）**:
   - 実装計画（Implementation Plan）の作成時や機能開発時、ユーザーへ逐次確認（Proceed待ち）を行わず、コードの作成・修正・動作検証・Gitコミットまで一気に全自動で自律実行すること。

7. **Notion風 UIデザイン方針（厳守）**:
   - **トーン＆マナー**: Notionのように「白と上質なグレー（#F8FAFC, #F1F5F9）」をベースにした洗練されたミニマルデザイン。
   - **余白美**: 情報同士の間にゆったりとした美しい余白（Padding / Margin）を配置し、圧倒的に読みやすくする。
   - **視覚アクセント**: アイコンや見出しに親しみやすい「絵文字アイコン (🗣️, 🎵, 💡, 📚, 🏆)」を配置。
   - **繊細な1px枠線とホバーアニメーション**: カードやボックスは繊細な1pxボーダー（`#E2E8F0`）で囲み、マウスホバー時に少しだけ上品に浮き上がるシャドウ（`box-shadow: 0 10px 25px -5px rgba(0,0,0,0.05)`）を適用すること。

8. **クイズ問題数の最適化・100%全要素網羅ルール**:
   - 各学習ステップ/ページで登場するすべての文字（子音・母音）および主要単語がクイズで1回も漏れることなく「100%完全網羅」される問題数（例: 8文字＋主要単語で計10〜12問など）を、ページの内容量に合わせて動的・適切に設計・実装すること。
   - 繰り返し挑戦時の学習効果を高めるため、クイズ開始時の「問題順序および選択肢の位置の自動シャッフル機能」を常時適用すること。

9. **Vercel自動デプロイ禁止ルール（ローカル検証のみ）**:
   - Vercelなどの外部デプロイコマンド（`npx vercel --prod` や `vercel` 等）はユーザーからの明示的な要望がない限り絶対に使用・実行しないこと。
   - コードの動作確認・表示検証はローカル環境（`npm run dev` や `npm run build` 等）のみで行うこと。

10. **純粋な静的ページ（Static HTML/CSS/JS）完結方針**:
   - 本プロジェクトはすべて純粋な静的HTML・CSS・JavaScript（Vanilla JS）で完結するように設計すること。
   - バックエンドサーバーや動的DB処理などの複雑な仕組みは一切持ち込まず、ブラウザでHTMLを直接開くだけで100%完全動作するシンプルな静的構成を維持すること。

11. **台湾華語と北京普通語（大陸）の違いコラム・対比併記ルール（コンテンツ内限定）**:
   - トップページ（ランディングページ）には余計な比較カードを載せず、各学習ステップ/詳細カードの中に自然な「🇹🇼 💡 台湾華語コラム（大陸との違い）」として組み込むこと。
   - 台湾華語の表現をメインで学習させつつ、台湾と大陸で表現が異なる単語や言い回しについては、**必ず「🇨🇳 大陸での対応表現」もコラムや補足カード内に明確に併記・追記**すること。
   - （例: タクシー 🇹🇼 `計程車` vs 🇨🇳 `出租車`, お弁当 🇹🇼 `便當` vs 🇨🇳 `盒飯`, ホテル 🇹🇼 `飯店` vs 🇨🇳 `酒店`, 動画 🇹🇼 `影片` vs 🇨🇳 `視頻`, コンビニ 🇹🇼 `超商` vs 🇨🇳 `便利店`, 語尾の助詞 `喔/啦/啊` の有無等）

12. **コンテンツ非表示（空白画面）防止策: 「Pure Static First」ルールの厳守**:
   - JSの動的 `innerHTML` 描画に依存して学習コンテンツを表示する設計を完全に排除し、すべてのHTMLファイル自体に100%完全な静的HTMLカード（文字・フレーズ・豆知識）をあらかじめ直書きしておくこと。
   - JavaScriptの役割は「画面構築」ではなく、「既存の静的要素への音声再生イベントおよびクイズの付与のみ」と制限すること。これにより、JSのロード遅延やエラーが発生した場合でも、学習コンテンツが画面から消えたり非表示になる事故を100%原理的に防止すること。

13. **「注音:」「ピンイン:」プレフィックスラベルの全廃（視認性優先のシンプルUI）**:
   - 単語カードや例文内で注音符号やピンインを表示する際、「注音: 」「ピンイン: 」といった不要で冗長なラベルテキストは一切付けず、注音符号およびピンイン文字列のみをシンプル・明瞭に表示すること。

14. **台湾華語ネイティブらしい自然なリアル表現の厳守 ＆ 大陸表現併記**:
   - メインで提示する例文や単語の選定時、中国大陸風の固い教科書表現を避け、台湾現地で実際にネイティブが日常・旅行・店舗で使う自然でリアルな台湾華語表現を厳選採用し、大陸での表現の違いも豆知識カード等でわかりやすく補足提示すること。

15. **各学習要素・カードにおける例文「最低2つ以上」の厳守**:
   - 各文字・各文法・各表現の学習カードには、単語例や例文を必ず「最低2つ以上（推奨2〜3例）」掲載すること。1つのみの提示で終わらせず、複数の使用例を示すことで定着効果を高めること。

16. **CATEGORY 2（会話・文法編）以降におけるピンイン非表示・注音符号単独表記ルール（厳守）**:
    - 注音符号マスターセクション（CATEGORY 1: STEP 1-1〜1-7）完了後の CATEGORY 2 以降のすべての学習カード・単語例・例文・クイズ選択肢においては、**ピンイン（アルファベット表記）を一切掲載せず、注音符号（ボポモフォ）のみを表示すること**。
    - 理由: ユーザーがアルファベットのピンインに依存せず、注音符号だけで台湾華語の単語や例文を読めるようにするため。

17. **新規HTMLページ作成時の音声イベントバインド ＆ 音声ブロック解除の徹底ルール（再発防止策）**:
    - 新しいHTMLページ（単語一覧ページ、学習ステップページ、追加ページなど）を制作する際は、必ず `appData.js`, `appCore.js` に加えて、音声要素（`.play-word-sound`, `.play-symbol-sound`）に対する明示的なイベントバインドJS（例: `categoryWords.js` や各ステップ専用JS）をセットで読み込ませること。
    - また、クリックイベントの同期タイミングで `speechSynthesis.resume()` を呼び出すことで、Mac Chrome や iOS Safari 等のブラウザによる自動再生ブロック・音声待機状態を原理的に予防・回避すること。

18. **クイズデータにおける正解キー（`targetSymbol`）と選択肢（`options`）の100%完全一致＆自動検証ルール（再発防止策）**:
    - クイズ問題を新たに作成・更新する際は、必ず正解キー `targetSymbol` が選択肢配列 `options` の中に **`options.includes(targetSymbol)` として100%完全一致**（文字表記・ルビ・言語キーの完全な同一性）で含まれていることを自動スクリプト等で必ず検証・保証すること。
    - 音声再生用のテキスト（`speechTarget`）と表示・判定用の選択肢キー（`targetSymbol`）を明確に分離し、ユーザーが正解の選択肢を選択した際に判定ミスや誤答扱いになる事故を永久に防止すること。










